// Placeholder router — implemented in a later prompt.
// Plan: vanilla hash-based routing (e.g. #/cassa, #/magazzino, #/chiusura).

export function initRouter() {
  // Intentionally a no-op for now; the entry point still wires this in
  // so future routes can be added without touching app.js.
}
